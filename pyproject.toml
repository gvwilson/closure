[project]
name = "closure"
description = "at the end of the day"
version = "0.3.0"
requires-python = ">=3.12"
dependencies = [
    "mccole>=2.10.0"
]

[tool.mccole]
skips = [
    "*~",
    "docs/**",
    "uv.lock"
]
